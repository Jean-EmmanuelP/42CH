'use client'

interface EventSubscribeModalProps {

}

export default function EventSubscribeModal({}: EventSubscribeModalProps) {
    return (
        <div>donner les bonnes props pour que tout fonctionne correctement et relier les bouttons accepter et refuser au back</div>
    );
}